import type { FAQ } from "@/types";

/**
 * Approved Q&A for the future AI avatar. Empty until verified copy exists.
 * The avatar must only answer from this source (plus other approved data files).
 */
export const faq: FAQ[] = [];
