# PUBLIC (safe in the browser)
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_ANALYTICS_ENABLED=true

# SERVER ONLY (never prefix with NEXT_PUBLIC)
# Leave empty locally: analytics validates but does not persist; contact returns 503.
DATABASE_URL=
CONTACT_EMAIL_TO=
EMAIL_API_KEY=
EMAIL_FROM=

